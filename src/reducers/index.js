const initialState = {
  posts: [],
  postDetail: {},
  comments: [],
  categories: []
};

function readable(state = initialState, action) {
  switch (action.type) {
    default:
    
      return state;
  }
}
export default readable;
